# requirements.txt - Complete Multi-Engine Pipeline (Whisper + PyAnnote + NeMo + VBX + SpeechBrain)

# PyTorch with CUDA support (adjust for your CUDA version)
torch>=2.0.0
torchaudio>=2.0.0

# OpenAI Whisper for speech recognition
openai-whisper>=20231117

# PyAnnote for speaker diarization (Option 1)
pyannote.audio>=3.1.0

# NeMo for advanced speaker diarization (Option 2)
nemo-toolkit[asr]>=1.20.0

# SpeechBrain for speaker diarization and embeddings (Option 3)
speechbrain>=0.5.16

# HuggingFace integration
transformers>=4.30.0
huggingface-hub>=0.16.0
datasets>=2.14.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.1
audioread>=3.0.0

# Advanced audio preprocessing
noisereduce>=2.0.0

# File format support
pydub>=0.25.1

# Progress bars and UI
tqdm>=4.65.0

# Excel and data processing
pandas>=2.0.0
openpyxl>=3.1.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Machine learning utilities
scikit-learn>=1.3.0

# Configuration management
omegaconf>=2.3.0
hydra-core>=1.3.0

# PyTorch Lightning (required by NeMo)
pytorch-lightning>=2.0.0

# Text processing
nltk>=3.8
text-unidecode>=1.3

# Development and testing
pytest>=7.4.0

# Additional dependencies for VBX-like implementation
matplotlib>=3.6.0

# CUDA Installation Notes:
# For CUDA 11.8 (most RTX 4060/4090):
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# For CUDA 12.1 (newer systems):
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# For CPU only:
# pip install torch torchaudio

# Installation Order Recommendations:
# 1. Install PyTorch first with appropriate CUDA version
# 2. Install basic dependencies:
#    pip install librosa soundfile pandas numpy scipy scikit-learn
# 3. Install speech processing libraries:
#    pip install openai-whisper pyannote.audio speechbrain
# 4. Install NeMo (may require specific versions):
#    pip install nemo-toolkit[asr]==1.20.0
# 5. Install remaining dependencies

# SpeechBrain Installation Notes:
# SpeechBrain works well with most PyTorch versions
# For best compatibility: pip install speechbrain==0.5.16

# VBX Implementation Notes:
# The VBX engine uses alternative implementations with available libraries
# No separate VBX installation required - it adapts to available tools

# NeMo Installation Notes:
# NeMo requires specific versions of dependencies
# If you encounter conflicts, install NeMo separately:
# pip install nemo-toolkit[asr]==1.20.0

# Alternative NeMo installation (if main fails):
# pip install Cython
# pip install git+https://github.com/NVIDIA/NeMo.git@main#egg=nemo_toolkit[asr]

# PyAnnote Authentication:
# 1. Create HuggingFace account: https://huggingface.co/
# 2. Accept license: https://huggingface.co/pyannote/speaker-diarization-3.1
# 3. Get access token: https://huggingface.co/settings/tokens
# 4. Login: huggingface-cli login

# GPU Memory Requirements:
# - Whisper Large: ~2GB VRAM
# - PyAnnote: ~1-2GB VRAM
# - SpeechBrain: ~1-2GB VRAM
# - NeMo: ~2-4GB VRAM
# - VBX (alternative): ~0.5-1GB VRAM